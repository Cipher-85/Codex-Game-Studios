# Codex Game Studios does not configure a funding destination by default.
# Fork owners can replace this comment-only file with GitHub-supported
# funding keys when they have an active project-specific funding account.
